name = "Nanaloveyuki/sync"

version = "0.2.1"

description = "Minimal native concurrency primitives for MoonBit."

repository = "https://github.com/Nanaloveyuki/sync"

license = "Apache-2.0"

readme = "README.md"

keywords = [ "moonbit", "concurrency", "thread", "channel" ]

preferred_target = "native"

source = "src"
